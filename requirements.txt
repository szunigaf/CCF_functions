# CCF Analysis Project Requirements
# Python 3.9+ compatible versions
# Based on actual code dependencies analysis

# Core scientific computing (required)
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.3.0

# Astronomy-specific packages (required)
astropy>=4.0.0

# Parallel processing (required)
pathos>=0.3.0

# Jupyter notebook support (optional, for interactive analysis)
jupyter>=1.0.0
notebook>=6.4.0
ipywidgets>=7.6.0

# Development and testing (optional)
pytest>=6.0.0
black>=22.0.0

# Note: Standard library modules are included with Python
# sys, os, glob, time, argparse, configparser, warnings, multiprocessing
# gc, copy, math, re, collections
